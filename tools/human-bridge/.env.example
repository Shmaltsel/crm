TELEGRAM_BOT_TOKEN=your-bot-token
TELEGRAM_CHAT_ID=your-chat-id

# Transport: "stdio" (default, single agent) or "sse" (multi-agent via StreamableHTTP)
HB_TRANSPORT=stdio
# HB_SSE_PORT=3100  # port for StreamableHTTP server (when HB_TRANSPORT=sse)
