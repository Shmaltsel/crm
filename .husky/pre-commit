#!/usr/bin/env sh
# Pre-commit: charset-guard → affected-first тести.
# Charset-guard виконується через node напряму (не pnpm) для сумісності.
node tools/queue-tools/charset-guard.js
if [ $? -ne 0 ]; then
  echo "❌ Charset-guard не пройшов — коміт заблоковано"
  exit 1
fi
pnpm test:affected
